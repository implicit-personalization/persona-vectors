### Run K-means on persona-vectors

- Then plot the Loss for different values of K (scree plot) and get a reasonable value of K (perhaps K-means++)
- Do it for each K several time to make sure we are arriving to something close to the global minima

-> Try to then interprete the results in a reasnoable way !
